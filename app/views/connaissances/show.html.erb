<div class="container mx-auto my-8 px-4">
    <p class="text-lg font-semibold">Nom:</p>
  <p class="leading-normal"><%= @connaissance.nom %></p>

    <p class="text-lg font-semibold">Description:</p>
  <p class="leading-normal"><%= @connaissance.description %></p>

    <div class="flex items-center justify-start mt-6">
    <%= link_to 'Modifier', edit_connaissance_path(@connaissance), class: "btn btn-default mr-4" %>
    <%= link_to 'Retour', connaissances_path, class: "btn btn-default" %>
  </div>
</div>
