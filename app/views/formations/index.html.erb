<div class="container mx-auto my-8 px-4">
  <div class="flex justify-between items-center mb-4">
    <h1 class="h2">Formations</h1>

    <% if @formations.exists? %>
      <%= link_to 'Ajouter formation', new_formation_path, class: "btn btn-default" %>
    <% end %>
  </div>

  <% if @formations.exists? %>
    <div class="bg-white rounded shadow">
      <table class="w-full">
        <thead>
          <tr>
            <th class="p-3 uppercase text-left text-xs text-gray-700">Formation</th>

            <th class="p-3 uppercase text-left text-xs text-gray-700">Actions</th>
          </tr>
        </thead>

        <tbody>
        <% @formations.each do |formation| %>
          <tr class="group border-t border-gray-400 hover:bg-gray-100">
                          <td class="p-3"><%= formation.formation %></td>
            
            <td>
              <%= link_to "Voir", formation, class: "btn btn-default" %>
              <%= link_to "Modifier", edit_formation_path(formation), class: "btn btn-default" %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>

  <% else %>
    <div class="bg-white rounded shadow flex items-center justify-between p-8">
      <div class="flex-1 text-center">
        <p class="text-xl font-semibold mb-4">Ajouter une nouvelle formation</p>
      <%= link_to 'Ajouter formation', new_formation_path, class: "btn btn-default" %>
      </div>
    </div>
  <% end %>
</div>
