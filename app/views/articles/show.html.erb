<div class="container mx-auto my-8 px-4">
    <p class="text-lg font-semibold">Titre:</p>
  <p class="leading-normal"><%= @article.title %></p>

    <p class="text-lg font-semibold">Texte:</p>
  <p class="leading-normal"><%= @article.body %></p>

    <div class="flex items-center justify-start mt-6">
    <%= link_to 'Modifier', edit_article_path(@article), class: "btn btn-default mr-4" %>
    <%= link_to 'Retour', articles_path, class: "btn btn-default" %>
  </div>
</div>
